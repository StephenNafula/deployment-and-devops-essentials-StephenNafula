# Example backend environment variables
# Copy to .env and update values when running locally or in your hosting platform
PORT=5000
# e.g. MONGO_URI=mongodb+srv://user:pass@cluster0.mongodb.net/dbname
MONGO_URI=
# Example use:
# MONGO_URI=mongodb+srv://<DB_USER>:<DB_PASS>@cluster0.wrwfhjn.mongodb.net/mern-app?retryWrites=true&w=majority
# Optional: specify database name and pool size
MONGO_DB_NAME=
MONGO_MAX_POOL_SIZE=10
# NODE_ENV=production
